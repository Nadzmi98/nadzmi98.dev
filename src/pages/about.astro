---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getAboutContent } from '../lib/site-content';
const { data, Content } = await getAboutContent();
---
<BaseLayout title={`${data.title} | Nadzmi Writes`}>
  <article class="prose">
    <h1>{data.title}</h1>
    <Content />
  </article>
</BaseLayout>
